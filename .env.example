# MongoDB Configuration
MONGODB_URI=mongodb+srv://crusherstress_db_user:fZZzqeaVL9KzQiYj@cluster0.q0ib2e2.mongodb.net/?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here

# Server Configuration
PORT=3001

# Razorpay Configuration (Get these from https://dashboard.razorpay.com/)
# Backend (server) variables
RAZORPAY_KEY_ID=rzp_test_your_key_id_here
RAZORPAY_SECRET=your_razorpay_secret_here

# Frontend (Vite) variable - safe to expose in browser
VITE_RAZORPAY_KEY_ID=${RAZORPAY_KEY_ID}

# Online payment amount cap (in INR) to avoid gateway limits (default: 100000)
MAX_ONLINE_PAYMENT_INR=100000

# Email Configuration (Required for Password Reset)
# For Gmail (recommended for development):
EMAIL_SERVICE=gmail
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# For other SMTP services:
# EMAIL_SERVICE=custom
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# EMAIL_USER=your-email@domain.com
# EMAIL_PASS=your-password

# How to set up Gmail App Password:
# 1. Enable 2-factor authentication on your Google account
# 2. Go to Google Account settings > Security > App passwords
# 3. Generate a new app password for "Mail"
# 4. Use that 16-character password as EMAIL_PASS (not your regular Gmail password)

# Other Configuration
NODE_ENV=development